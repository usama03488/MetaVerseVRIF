using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Instantiator : MonoBehaviour
{
   /* public GameObject KnifeGameObject;

    private void Start()
    {
        GameObject Temp = Instantiate(KnifeGameObject,transform);
        
    }

    public void InstantiatorKnifeGame(GameObject DestoryableObject)
    {
     foreach(var Coin in FindObjectsOfType<CoinController>())
        {
            Destroy(Coin.gameObject);
        }
        Destroy(DestoryableObject);
        Invoke(nameof(InvokeInstantiate), 1f);
    }
    void InvokeInstantiate()
    {
        GameObject Temp = Instantiate(KnifeGameObject, transform);
       

    }*/

}
